#pragma once
// ---------------------------------------------------------------------
//  Template. Copy this file to secrets.h and fill in your WiFi details.
//  secrets.h is gitignored, so your credentials never get committed.
// ---------------------------------------------------------------------
#define WIFI_SSID  "YOUR_WIFI_SSID"
#define WIFI_PASS  "YOUR_WIFI_PASSWORD"

// OTA (over-the-air update) password. Used as HTTP basic-auth password
// (username "admin") when pushing firmware to /update.
#define OTA_PASSWORD  "segclock"
